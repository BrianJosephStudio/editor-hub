{
    "name": "editor-hub",
    "private": true,
    "workspaces": [
        "libs/*",
        "server",
        "apps/*"
    ],
    "scripts": {
        "deploy": "sudo docker compose down -v && NODE_ENV=production sudo docker compose --env-file .env.production up --build",
        "build:libs": "./workflows/build-libs.sh"
    }
}